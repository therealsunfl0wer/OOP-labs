// Lab 6 - Sender component of a message-driven multi-process system.
// Two independent executables (sender/receiver) exchange messages,
// e.g. over a local socket (QLocalSocket) or UDP (QUdpSocket).
#include <QCoreApplication>

int main(int argc, char *argv[]) {
    QCoreApplication app(argc, argv);
    // TODO: connect to the receiver and send messages.
    return 0;
}
